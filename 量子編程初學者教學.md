# 量子編程初學者教學

## 目錄
1. [開始之前](#開始之前)
2. [Qiskit 簡介](#qiskit-簡介)
3. [環境設置](#環境設置)
4. [第一個量子程式：Hadamard 測量](#第一個量子程式hadamard-測量)
5. [量子電路基礎](#量子電路基礎)
6. [常用量子閘操作](#常用量子閘操作)
7. [進階範例](#進階範例)
8. [除錯與優化](#除錯與優化)
9. [實用技巧](#實用技巧)

---

## 開始之前

### 先修知識

- **Python 基礎**：變數、函數、迴圈
- **量子計算基礎**：建議先閱讀《量子電腦初學者教學.md》
- **基本線性代數**：向量、矩陣（有助於理解，但非必需）

### 學習目標

完成本教學後，您將能夠：
- ✓ 安裝並配置 Qiskit 環境
- ✓ 創建和操作量子電路
- ✓ 使用量子閘創建疊加態和糾纏態
- ✓ 運行量子模擬並分析結果
- ✓ 理解量子程式的基本結構

---

## Qiskit 簡介

**Qiskit** 是 IBM 開發的開源量子計算框架，是目前最流行的量子編程工具之一。

### Qiskit 的優勢

- 🆓 **開源免費**
- 🌐 **社群活躍**：豐富的文件和範例
- 🖥️ **模擬器**：可以在本地電腦模擬量子電路
- ☁️ **雲端存取**：可以連接到真實的 IBM 量子電腦
- 🐍 **Python 整合**：使用熟悉的 Python 語法

### Qiskit 的組成

| 模組 | 功能 |
|------|------|
| `qiskit` | 核心功能：建立電路、量子閘 |
| `qiskit_aer` | 本地模擬器 |
| `qiskit_ibm_runtime` | 連接到 IBM 量子電腦 |
| `qiskit_algorithms` | 量子演算法實作 |

---

## 環境設置

### 安裝 Qiskit

使用 pip 安裝完整版 Qiskit：

```bash
pip install qiskit[all]
```

### 驗證安裝

```python
import qiskit
print(qiskit.__version__)
```

### 推薦的開發環境

- **Jupyter Notebook**：適合學習和實驗
- **VS Code**：適合開發完整專案
- **Google Colab**：雲端環境，無需本地安裝

---

## 第一個量子程式：Hadamard 測量

讓我們從一個簡單但完整的範例開始，展示量子疊加態的基本概念。

### 完整程式碼

```python
# 匯入必要的模組
from qiskit import QuantumCircuit
from qiskit_aer.primitives import Sampler

# 建立一個只有 1 個 qubit 和 1 個經典位元（用來儲存測量結果）
qc = QuantumCircuit(1, 1)

# 對 qubit 套用 Hadamard Gate,讓它進入疊加態
qc.h(0)

# 測量 qubit，結果存到第 0 個經典位元
qc.measure(0, 0)

# 用模擬器執行
sampler = Sampler()
job = sampler.run(qc, shots=1000)
result = job.result()

# 顯示測量結果
counts = result.quasi_dists[0].binary_probabilities()
with open("results.txt", "w", encoding="utf-8") as f:
    f.write(str(counts))

print(counts)
```

### 程式碼解析

#### 1. 匯入模組

```python
from qiskit import QuantumCircuit
from qiskit_aer.primitives import Sampler
```

- `QuantumCircuit`：用於建立量子電路
- `Sampler`：Qiskit Aer 的模擬器，用於執行電路

#### 2. 建立量子電路

```python
qc = QuantumCircuit(1, 1)
```

參數說明：
- **第一個 1**：量子位元（qubit）的數量
- **第二個 1**：經典位元（classical bit）的數量

經典位元用於儲存測量結果。

#### 3. 應用 Hadamard 閘

```python
qc.h(0)
```

- `h(0)`：對第 0 個 qubit 應用 Hadamard 閘
- **效果**：將 `|0⟩` 轉換為 `(|0⟩ + |1⟩)/√2`（均等疊加態）

這意味著測量時有 50% 機率得到 0，50% 機率得到 1。

#### 4. 測量 Qubit

```python
qc.measure(0, 0)
```

- **第一個 0**：要測量的 qubit（索引 0）
- **第二個 0**：儲存結果的經典位元（索引 0）

測量會使疊加態坍縮為 0 或 1。

#### 5. 執行模擬

```python
sampler = Sampler()
job = sampler.run(qc, shots=1000)
result = job.result()
```

- `Sampler()`：創建模擬器實例
- `shots=1000`：運行電路 1000 次
- 為什麼需要多次？因為量子測量是機率性的

#### 6. 獲取結果

```python
counts = result.quasi_dists[0].binary_probabilities()
```

結果格式類似：
```python
{'0': 0.501, '1': 0.499}
```

表示約 50% 得到 0，50% 得到 1。

### 視覺化電路

```python
# 顯示電路圖
print(qc.draw())
```

輸出：
```
     ┌───┐┌─┐
q_0: ┤ H ├┤M├
     └───┘└╥┘
c: 1/══════╩═
           0
```

說明：
- `H`：Hadamard 閘
- `M`：測量操作
- 線條連接到經典位元 0

### 預期結果

運行 1000 次後，您應該看到類似的結果：
```python
{'0': 0.497, '1': 0.503}
```

由於是隨機的，每次運行結果會略有不同，但應該接近 50/50 的分布。

---

## 量子電路基礎

### 創建電路

#### 基本語法

```python
# 只有 qubits
qc = QuantumCircuit(3)  # 3 個 qubits

# qubits + 經典位元
qc = QuantumCircuit(3, 3)  # 3 個 qubits, 3 個經典位元

# 使用暫存器
from qiskit import QuantumRegister, ClassicalRegister

qr = QuantumRegister(3, 'q')
cr = ClassicalRegister(3, 'c')
qc = QuantumCircuit(qr, cr)
```

### 索引規則

Qiskit 使用 **從 0 開始** 的索引：

```python
qc = QuantumCircuit(3, 3)
# qubits: 0, 1, 2
# 經典位元: 0, 1, 2
```

### 電路操作

#### 添加量子閘

```python
qc.h(0)      # Hadamard 在 qubit 0
qc.x(1)      # Pauli-X 在 qubit 1
qc.cx(0, 1)  # CNOT: 控制=0, 目標=1
```

#### 測量

```python
# 測量單個 qubit
qc.measure(0, 0)

# 測量所有 qubits
qc.measure_all()

# 測量特定範圍
qc.measure([0, 1], [0, 1])
```

#### 電路組合

```python
qc1 = QuantumCircuit(2)
qc1.h(0)

qc2 = QuantumCircuit(2)
qc2.cx(0, 1)

# 組合電路
qc = qc1.compose(qc2)
```

---

## 常用量子閘操作

### 單量子位元閘

#### 1. Pauli 閘

```python
# X 閘（NOT 閘）：翻轉 qubit
qc.x(0)  # |0⟩ → |1⟩, |1⟩ → |0⟩

# Y 閘：翻轉並改變相位
qc.y(0)

# Z 閘：相位翻轉
qc.z(0)  # |1⟩ → -|1⟩
```

#### 2. Hadamard 閘

```python
# 創建疊加態
qc.h(0)  # |0⟩ → (|0⟩ + |1⟩)/√2
```

**應用場景**：
- 創建均等疊加態
- 量子演算法的初始化
- 干涉效應

#### 3. 旋轉閘

```python
import math

# 繞 X 軸旋轉
qc.rx(math.pi/4, 0)

# 繞 Y 軸旋轉
qc.ry(math.pi/4, 0)

# 繞 Z 軸旋轉
qc.rz(math.pi/4, 0)
```

#### 4. Phase 閘

```python
# S 閘（相位 π/2）
qc.s(0)

# T 閘（相位 π/4）
qc.t(0)

# 自訂相位
qc.p(math.pi/8, 0)
```

### 雙量子位元閘

#### 1. CNOT（受控非閘）

```python
# CNOT: 控制 qubit=0, 目標 qubit=1
qc.cx(0, 1)
```

**真值表**：
```
|00⟩ → |00⟩
|01⟩ → |01⟩
|10⟩ → |11⟩  # 控制為 1 時，翻轉目標
|11⟩ → |10⟩
```

**應用**：創建糾纏態

#### 2. 其他受控閘

```python
# Controlled-Z
qc.cz(0, 1)

# Controlled-Y
qc.cy(0, 1)

# SWAP：交換兩個 qubit 的狀態
qc.swap(0, 1)
```

### 三量子位元閘

#### Toffoli 閘（CCNOT）

```python
# 雙控制 NOT
qc.ccx(0, 1, 2)  # 控制: 0和1, 目標: 2
```

**用途**：量子邏輯運算、錯誤修正

---

## 進階範例

### 範例 1：創建貝爾態（糾纏態）

```python
from qiskit import QuantumCircuit
from qiskit_aer.primitives import Sampler

# 創建 2-qubit 電路
qc = QuantumCircuit(2, 2)

# 步驟 1：讓第一個 qubit 進入疊加態
qc.h(0)

# 步驟 2：使用 CNOT 創建糾纏
qc.cx(0, 1)

# 測量兩個 qubits
qc.measure([0, 1], [0, 1])

# 執行
sampler = Sampler()
job = sampler.run(qc, shots=1000)
result = job.result()
counts = result.quasi_dists[0].binary_probabilities()

print(counts)
# 預期結果: {'00': ~0.5, '11': ~0.5}
# 注意：不會出現 '01' 或 '10'，因為兩個 qubits 是糾纏的
```

**電路圖**：
```
     ┌───┐     ┌─┐
q_0: ┤ H ├──■──┤M├───
     └───┘┌─┴─┐└╥┘┌─┐
q_1: ─────┤ X ├─╫─┤M├
          └───┘ ║ └╥┘
c: 2/═══════════╩══╩═
                0  1
```

**解釋**：
1. Hadamard 讓 qubit 0 變成 `(|0⟩ + |1⟩)/√2`
2. CNOT 創建糾纏態 `(|00⟩ + |11⟩)/√2`
3. 測量結果：要麼都是 0，要麼都是 1

### 範例 2：量子疊加的干涉

```python
from qiskit import QuantumCircuit
from qiskit_aer.primitives import Sampler

qc = QuantumCircuit(1, 1)

# 應用兩次 Hadamard
qc.h(0)  # |0⟩ → (|0⟩ + |1⟩)/√2
qc.h(0)  # → |0⟩

qc.measure(0, 0)

# 執行
sampler = Sampler()
job = sampler.run(qc, shots=1000)
result = job.result()
counts = result.quasi_dists[0].binary_probabilities()

print(counts)
# 預期結果: {'0': ~1.0}
# 兩次 Hadamard 互相抵消！
```

**重點**：展示了量子干涉效應

### 範例 3：多 Qubit 疊加態

```python
from qiskit import QuantumCircuit
from qiskit_aer.primitives import Sampler

# 創建 3-qubit 電路
qc = QuantumCircuit(3, 3)

# 所有 qubits 都應用 Hadamard
qc.h(0)
qc.h(1)
qc.h(2)

# 測量全部
qc.measure([0, 1, 2], [0, 1, 2])

# 執行
sampler = Sampler()
job = sampler.run(qc, shots=1000)
result = job.result()
counts = result.quasi_dists[0].binary_probabilities()

print(counts)
# 預期結果: 8 種狀態均等分布
# {'000': ~0.125, '001': ~0.125, ..., '111': ~0.125}
```

**解釋**：
- 3 個 qubits，每個都處於疊加態
- 總共 2³ = 8 種可能狀態
- 每種狀態機率約 12.5%

---

## 除錯與優化

### 視覺化工具

#### 1. 繪製電路

```python
# 文字格式
print(qc.draw())

# ASCII 藝術
print(qc.draw('text'))

# Matplotlib（需安裝 matplotlib）
qc.draw('mpl')

# LaTeX（高品質）
qc.draw('latex')
```

#### 2. 查看電路資訊

```python
# 電路深度（層數）
print(f"深度: {qc.depth()}")

# 量子閘數量
print(f"閘數: {qc.size()}")

# qubit 數量
print(f"Qubits: {qc.num_qubits}")

# 詳細統計
print(qc.count_ops())
```

#### 3. 視覺化測量結果

```python
from qiskit.visualization import plot_histogram
import matplotlib.pyplot as plt

# 繪製直方圖
plot_histogram(counts)
plt.show()
```

### 常見錯誤

#### 1. 索引超出範圍

```python
qc = QuantumCircuit(2, 2)
qc.h(3)  # ❌ 錯誤！只有 qubit 0 和 1
```

**解決方法**：檢查索引是否在範圍內

#### 2. 測量前未連接經典位元

```python
qc = QuantumCircuit(2)  # 沒有經典位元！
qc.h(0)
qc.measure(0, 0)  # ❌ 錯誤！
```

**解決方法**：創建電路時包含經典位元

#### 3. 忘記測量

```python
qc = QuantumCircuit(1, 1)
qc.h(0)
# 沒有 measure！

sampler = Sampler()
job = sampler.run(qc, shots=1000)  # ⚠️ 不會有測量結果
```

**解決方法**：總是添加 `measure` 操作

### 效能優化

#### 1. 減少量子閘數量

```python
# ❌ 低效
qc.x(0)
qc.x(0)  # 兩次 X 等於沒做

# ✅ 優化
# 移除重複的閘
```

#### 2. 使用電路優化

```python
from qiskit import transpile

# 優化電路
optimized_qc = transpile(qc, optimization_level=3)
print(f"原始深度: {qc.depth()}")
print(f"優化後深度: {optimized_qc.depth()}")
```

#### 3. 調整 shots 數量

```python
# 快速測試：少量 shots
job = sampler.run(qc, shots=100)

# 準確結果：大量 shots
job = sampler.run(qc, shots=10000)
```

**建議**：
- 開發時使用 100-1000 shots
- 最終結果使用 10000+ shots

---

## 實用技巧

### 1. 儲存和載入電路

```python
# 儲存為 QASM 格式
qasm_str = qc.qasm()
with open('circuit.qasm', 'w') as f:
    f.write(qasm_str)

# 從 QASM 載入
from qiskit import QuantumCircuit
qc = QuantumCircuit.from_qasm_file('circuit.qasm')
```

### 2. 參數化電路

```python
from qiskit.circuit import Parameter

# 創建參數
theta = Parameter('θ')

qc = QuantumCircuit(1)
qc.ry(theta, 0)

# 綁定參數值
import math
bound_qc = qc.bind_parameters({theta: math.pi/4})
```

**用途**：變分量子演算法（VQE、QAOA）

### 3. 條件操作

```python
from qiskit import QuantumCircuit, ClassicalRegister, QuantumRegister

qr = QuantumRegister(2, 'q')
cr = ClassicalRegister(2, 'c')
qc = QuantumCircuit(qr, cr)

# 測量
qc.measure(qr[0], cr[0])

# 根據測量結果執行操作
qc.x(qr[1]).c_if(cr[0], 1)  # 如果 cr[0] == 1，則執行 X
```

### 4. 自訂閘

```python
from qiskit.circuit import Gate
import numpy as np

# 定義自訂矩陣
my_gate = Gate('MyGate', 1, [])
my_gate = my_gate.to_matrix()

# 使用
qc.unitary(my_gate, [0], label='MyGate')
```

### 5. 批次執行

```python
# 創建多個電路
circuits = []
for angle in [0, np.pi/4, np.pi/2]:
    qc = QuantumCircuit(1, 1)
    qc.ry(angle, 0)
    qc.measure(0, 0)
    circuits.append(qc)

# 一次執行所有電路
sampler = Sampler()
job = sampler.run(circuits, shots=1000)
results = job.result()
```

---

## 實踐練習

### 練習 1：單 Qubit 實驗

**任務**：測試不同角度的旋轉閘

```python
import math
from qiskit import QuantumCircuit
from qiskit_aer.primitives import Sampler

angles = [0, math.pi/4, math.pi/2, 3*math.pi/4, math.pi]

for angle in angles:
    qc = QuantumCircuit(1, 1)
    qc.ry(angle, 0)  # 繞 Y 軸旋轉
    qc.measure(0, 0)

    sampler = Sampler()
    job = sampler.run(qc, shots=1000)
    counts = job.result().quasi_dists[0].binary_probabilities()

    print(f"角度 {angle:.2f}: {counts}")
```

**觀察**：旋轉角度如何影響測量機率？

### 練習 2：創建 GHZ 態

**任務**：創建 3-qubit 糾纏態

```python
qc = QuantumCircuit(3, 3)

# 你的代碼：
# 1. 對 qubit 0 應用 Hadamard
# 2. 使用 CNOT 創建糾纏（0→1, 1→2）
# 3. 測量所有 qubits

# 預期結果：只出現 '000' 和 '111'
```

<details>
<summary>解答</summary>

```python
qc = QuantumCircuit(3, 3)
qc.h(0)
qc.cx(0, 1)
qc.cx(1, 2)
qc.measure([0, 1, 2], [0, 1, 2])

sampler = Sampler()
job = sampler.run(qc, shots=1000)
counts = job.result().quasi_dists[0].binary_probabilities()
print(counts)
```
</details>

### 練習 3：量子隨機數生成器

**任務**：生成真正的隨機數

```python
def quantum_random_bit():
    """使用量子疊加生成隨機位元"""
    qc = QuantumCircuit(1, 1)
    # 你的代碼
    pass

def quantum_random_int(n_bits):
    """生成 n 位元的隨機整數"""
    # 你的代碼
    pass

# 測試
print(quantum_random_int(8))  # 0-255 的隨機數
```

---

## 連接到真實量子電腦

### IBM Quantum 平台

1. **註冊帳號**：https://quantum-computing.ibm.com/
2. **獲取 API Token**
3. **配置 Qiskit**：

```python
from qiskit_ibm_runtime import QiskitRuntimeService

# 儲存帳號（只需一次）
QiskitRuntimeService.save_account(channel="ibm_quantum", token="YOUR_TOKEN")

# 載入服務
service = QiskitRuntimeService()

# 查看可用的量子電腦
print(service.backends())

# 選擇後端
backend = service.backend("ibm_brisbane")

# 執行電路
sampler = Sampler(backend=backend)
job = sampler.run(qc, shots=1000)
```

### 注意事項

- 🕐 **等待時間**：真實量子電腦有排隊
- 💰 **免費額度**：每月有限的免費運行時間
- ❌ **錯誤率**：真實硬體有噪音和錯誤
- 📊 **結果差異**：與模擬器結果可能不同

---

## 學習資源

### 官方文件
- [Qiskit 官方文件](https://qiskit.org/documentation/)
- [Qiskit 教學](https://qiskit.org/learn/)
- [IBM Quantum Learning](https://learning.quantum.ibm.com/)

### 互動學習
- IBM Quantum Composer：視覺化拖拉電路設計
- Qiskit Textbook：互動式教科書

### 社群
- [Qiskit Slack](https://qiskit.slack.com/)
- [Stack Overflow - qiskit 標籤](https://stackoverflow.com/questions/tagged/qiskit)
- [GitHub Discussions](https://github.com/Qiskit/qiskit/discussions)

### 進階主題
- 量子演算法：Grover、Shor、VQE
- 量子機器學習
- 量子錯誤修正
- 量子化學模擬

---

## 總結

### 關鍵要點

✓ **量子電路**是量子程式的基本結構
✓ **量子閘**用於操作 qubits
✓ **測量**將量子態轉換為經典資訊
✓ **多次運行**（shots）對於統計結果很重要
✓ **Hadamard 閘**創建疊加態，**CNOT 閘**創建糾纏態

### 下一步學習

1. **深入量子演算法**：
   - Grover 搜尋演算法
   - Shor 因數分解演算法
   - 量子傅立葉轉換

2. **探索實際應用**：
   - 量子化學（VQE）
   - 優化問題（QAOA）
   - 量子機器學習

3. **實踐專案**：
   - 實作簡單的量子遊戲
   - 量子加密協議
   - 連接到真實量子硬體

---

## 完整範例：整合所有概念

```python
"""
完整範例：量子疊加和糾纏的展示
"""
from qiskit import QuantumCircuit
from qiskit_aer.primitives import Sampler
from qiskit.visualization import plot_histogram
import matplotlib.pyplot as plt

def demonstrate_quantum_phenomena():
    """展示量子疊加和糾纏"""

    # === 實驗 1：單 Qubit 疊加 ===
    print("=== 實驗 1：單 Qubit 疊加 ===")
    qc1 = QuantumCircuit(1, 1)
    qc1.h(0)
    qc1.measure(0, 0)

    sampler = Sampler()
    job1 = sampler.run(qc1, shots=1000)
    counts1 = job1.result().quasi_dists[0].binary_probabilities()
    print(f"結果: {counts1}")
    print(qc1.draw())

    # === 實驗 2：雙 Qubit 糾纏 ===
    print("\n=== 實驗 2：雙 Qubit 糾纏（貝爾態）===")
    qc2 = QuantumCircuit(2, 2)
    qc2.h(0)
    qc2.cx(0, 1)
    qc2.measure([0, 1], [0, 1])

    job2 = sampler.run(qc2, shots=1000)
    counts2 = job2.result().quasi_dists[0].binary_probabilities()
    print(f"結果: {counts2}")
    print(qc2.draw())

    # === 實驗 3：三 Qubit GHZ 態 ===
    print("\n=== 實驗 3：三 Qubit GHZ 態 ===")
    qc3 = QuantumCircuit(3, 3)
    qc3.h(0)
    qc3.cx(0, 1)
    qc3.cx(1, 2)
    qc3.measure([0, 1, 2], [0, 1, 2])

    job3 = sampler.run(qc3, shots=1000)
    counts3 = job3.result().quasi_dists[0].binary_probabilities()
    print(f"結果: {counts3}")
    print(qc3.draw())

    return counts1, counts2, counts3

# 執行演示
if __name__ == "__main__":
    demonstrate_quantum_phenomena()
```

**運行這個程式**，您將看到量子計算的核心現象！

---

恭喜您完成量子編程初學者教學！現在您已經掌握了使用 Qiskit 進行量子編程的基礎知識。繼續實驗和學習，探索量子計算的無限可能！

🚀 Happy Quantum Computing! 🚀
